spring:
  profiles:
    active: local
  threads:
    virtual:
      enabled: true
  codec:
    max-in-memory-size: 10MB
  web:
    resources:
      chain:
        compressed: true
  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
server:
  port: 8000
  compression:
    enabled: true
  http2:
    enabled: true
sentry:
  dsn:
  traces-sample-rate: 0
jwt:
  secret: ${JWT_SECRET:JOONY.KIM!!demo-api%%jwt^^secret}
  access-token-expiration-minutes: 5
  refresh-token-expiration-minutes: 30
---
spring:
  config:
    activate:
      on-profile: local
  r2dbc:
    url: r2dbc:mysql://dev.baot.co.kr:3306/studydb?useSSL=false&serverTimezone=UTC&zeroDateTimeBehavior=convertToNull
    username: studydb
    password: studydb%#123
    pool:
      max-size: 3
      min-idle: 2
      initial-size: 2
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
server:
  port: 8000
sentry:
  logging:
    enabled: false
environment:
  app:
    test: test
jwt:
  access-token-expiration-minutes: 1440
---
spring:
  config:
    activate:
      on-profile: sandbox
  r2dbc:
    url:
    username:
    password:
    pool:
      max-size: 5
      min-idle: 3
      initial-size: 3
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
sentry:
  environment: sandbox
jwt:
  access-token-expiration-minutes: 1440
---
spring:
  config:
    activate:
      on-profile: qa
  r2dbc:
    url:
    username:
    password:
    pool:
      max-size: 10
      min-idle: 5
      initial-size: 5
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
sentry:
  environment: qa
---
spring:
  config:
    activate:
      on-profile: prod
  r2dbc:
    url:
    username:
    password:
    pool:
      max-size: 30
      min-idle: 10
      initial-size: 10
sentry:
  environment: prod
